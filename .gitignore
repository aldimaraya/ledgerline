node_modules/
dist/
data/
.env
*.db
*.db-wal
*.db-shm
docs/sample-response.json
.DS_Store

# local, unshared baseline figures
docs/BASELINE.local.md
*.local.md

# built PWA, produced by `npm run build` in web/
web/dist/

# Exported balance history from a previous provider — real balances, never commit
docs/*historic*.json

# import mapping — names real accounts
docs/import-map.local.json

# local backups of the live database
server/backups/

# staging area for moving the live database — real balances
transfer/
