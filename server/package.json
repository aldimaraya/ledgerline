{
  "name": "ledgerline-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node src/index.ts",
    "sync": "node src/cli/sync.ts",
    "test": "node --test src/**/*.test.ts",
    "backup": "node src/cli/backup.ts",
    "import-history": "node src/cli/import-history.ts"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/static": "^8.0.4",
    "fastify": "^5.2.0",
    "node-cron": "^3.0.3",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/node-cron": "^3.0.11",
    "typescript": "^5.7.3"
  }
}
